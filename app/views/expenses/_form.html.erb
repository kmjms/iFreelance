<%= form_with(model: expense, local: true) do |form| %>
  <% if expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.text_field :quantity, id: :expense_quantity %>
  </div>

  <div class="field">
    <%= form.label :process %>
    <%= form.text_field :process, id: :expense_process %>
  </div>

  <div class="field">
    <%= form.label :date_expenses %>
    <%= form.date_select :date_expenses, id: :expense_date_expenses %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, id: :expense_category %>
  </div>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state, id: :expense_state %>
  </div>

  <div class="field">
    <%= form.label :vinculation %>
    <%= form.check_box :vinculation, id: :expense_vinculation %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
