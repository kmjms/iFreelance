<%= form_with(model: type_job, local: true) do |form| %>
  <% if type_job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type_job.errors.count, "error") %> prohibited this type_job from being saved:</h2>

      <ul>
      <% type_job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :type_job_name %>
  </div>

  <div class="field">
    <%= form.label :skilsRequired %>
    <%= form.number_field :skilsRequired, id: :type_job_skilsRequired %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
