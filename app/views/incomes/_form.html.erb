<%= form_with(model: income, local: true) do |form| %>
  <% if income.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(income.errors.count, "error") %> prohibited this income from being saved:</h2>

      <ul>
      <% income.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :income_price %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :decription, id: :income_description %>
  </div>

  <div class="field">
      <%= form.label :project_id, class: "control-label" %>
      <%= form.collection_select(:project_id, @project, :id, :name, prompt: "Seleccione un tipo") %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
