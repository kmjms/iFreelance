<%= form_with(model: quotation, local: true) do |form| %>
  <% if quotation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quotation.errors.count, "error") %> prohibited this quotation from being saved:</h2>

      <ul>
      <% quotation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :quotationNumber %>
    <%= form.number_field :quotationNumber, id: :quotation_quotationNumber %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :quotation_price %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.number_field :duration, id: :quotation_duration %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :quotation_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
