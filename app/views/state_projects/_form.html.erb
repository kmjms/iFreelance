<%= form_with(model: state_project, local: true) do |form| %>
  <% if state_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(state_project.errors.count, "error") %> prohibited this state_project from being saved:</h2>

      <ul>
      <% state_project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name_state %>
    <%= form.text_field :name_state, id: :state_project_name_state %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :state_project_description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
